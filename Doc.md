user visits website and click on get started
server generated a uuid and assigned it to user as cookie
if(user already has cookie)
    use that to fetch file data uploaded by users
    give user access to user for new file/s upload
    if(user clicks on a file)
        user will be able to see its live status using web socket   
        if(user file is processed)
            user will be able to see a downloadable file updated with output sum value 